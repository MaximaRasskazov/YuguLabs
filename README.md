## 🚀 Как запустить сервер

1. Запуск приложения:
    ```bash
    go run cmd/app/main.go
    ```
2. Провекрка эндпоинтов:
    ```bash
    curl http://127.0.0.1:8000/info/server
    curl http://127.0.0.1:8000/info/client
    curl http://127.0.0.1:8000/info/database
    ```
3. Авто-Тестирование:
    ```bash
    go test ./internal/controller/... -v
    ```
## 📂 Структура проекта

```text
go-lab1/
├── cmd/
│   └── app/
│       └── main.go           # Точка входа, конфигурация (локаль, таймзона, роутер)
├── internal/
│   ├── controller/
│   │   ├── info.go           # HTTP хендлеры (прием запросов и отдача DTO)
│   │   └── info_test.go      # Изолированные Unit-тесты с моками
│   ├── dto/
│   │   └── info.go           # Data Transfer Objects (структуры JSON)
│   └── service/
│       └── info.go           # Бизнес-логика и работа с БД (или её имитация)
├── .env                      # Переменные окружения (исключён из git)
├── .gitignore                # Исключённые файлы
└── go.mod                    # Инициализация модуля Go
```
